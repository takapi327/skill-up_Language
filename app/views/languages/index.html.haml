.language__detail
  .menu
    .btn.active HTML&CSS
    .btn Ruby
    .btn JavaScript
    .btn PHP
    .btn Java
    .btn C
    .btn Python
    .btn other
  .contents
    .content.show
      .language__count
        ="HTML&CSS 1-#{@html.count}件表示"
      - @html.each do |language|
        = render partial: "language", locals: { language: language }
    .content
      .language__count
        ="Ruby 1-#{@ruby.count}件表示"
      - @ruby.each do |language|
        = render partial: "language", locals: { language: language }
    .content
      .language__count
        ="JavaScript 1-#{@javascript.count}件表示"
      - @javascript.each do |language|
        = render partial: "language", locals: { language: language }
    .content
      .language__count
        ="PHP 1-#{@php.count}件表示"
      - @php.each do |language|
        = render partial: "language", locals: { language: language }
    .content
      .language__count
        ="Java 1-#{@java.count}件表示"
      - @java.each do |language|
        = render partial: "language", locals: { language: language }
    .content
      .language__count
        ="C 1-#{@c.count}件表示"
      - @c.each do |language|
        = render partial: "language", locals: { language: language }
    .content
      .language__count
        ="Python 1-#{@python.count}件表示"
      - @python.each do |language|
        = render partial: "language", locals: { language: language }
    .content
      .language__count
        ="Other 1-#{@other.count}件表示"
      - @other.each do |language|
        = render partial: "language", locals: { language: language }
  .back-btn
    = link_to 'もどる', root_path